<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="color-scheme" content="light dark">
    <title>{{ title }} - {{ site.title }}</title>
    <link rel="shortcut icon" href="/assets/icon.png">
    <link rel="stylesheet" href="/styles/main.css">
    {% for stylesheet in stylesheets %}
    <link rel="stylesheet" href="{{ stylesheet }}">
    {% endfor %}
</head>
<body>
    <nav class="nav">
        <div class="container">
            <ul class="left">
            {% for item in nav.left %}
            <li class="nav-item {{ item.class }}">
                <a href="{{ item.url }}" {% if item.outside %}target="_blank"{% endif %}>{{ item.html | safe }}</a>
            </li>
            {% endfor %}
            </ul>
            <ul class="right">
            {% for item in nav.right %}
            <li class="nav-item {{ item.class }}">
                <a href="{{ item.url }}" {% if item.outside %}target="_blank"{% endif %}>{{ item.html | safe }}</a>
            </li>
            {% endfor %}
            </ul>
        </div>
    </nav>
    <div class="content">
        {{ content | safe }}
    </div>
    <footer class="footer">
        <div class="container">
            <a href="/" class="icon"><img src="/assets/icon.png"></a>
            <div class="license">Code licensed <a href="https://github.com/MrWillCom/rsa-cli/blob/main/LICENSE">MIT</a>, docs <a href="https://creativecommons.org/licenses/by-nc/4.0/">CC BY-NC 4.0</a>.</div>
            <div class="built-with">Built with <a href="https://www.11ty.dev/" target="_blank">11ty</a></div>
            <div class="powered-by">Powered by <a href="https://vercel.com/?utm_source=mrwillcom&utm_campaign=oss" target="_blank"><svg height="14" viewBox="0 0 75 65" fill="var(--color-text-primary)" style="transform: translateY(2px)"><path d="M37.59.25l36.95 64H.64l36.95-64z"></path></svg></a></div>
        </div>
    </footer>
    {% for script in scripts %}
    <script src="{{ script }}"></script>
    {% endfor %}
</body>
</html>